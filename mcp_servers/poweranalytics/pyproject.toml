[project]
name = "poweranalytics"
version = "0.1.0"
description = "MCP server for PowerAnalytics.jl — local Julia-based power system analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]
